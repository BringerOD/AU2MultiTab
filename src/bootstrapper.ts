// export default class Bootstrapper {

//     public static ConfigureServices(container: Containe) {
//         const httpClient = container.get(HttpClient);
    
//         container.registerSingleton(MainAPIClient, () => new MainAPIClient(environment.services.mainApi.url, httpClient));
//       }
// }
